class Solution {
    public int countSubarrays(int[] nums) {
        int count=0;
        for(int i=1; i+1<nums.length;i++){
            int a=nums[i-1], b=nums[i],c=nums[i+1];
            if(a+c==b/2.0){
                count++;
            }
        }
        return count;
        
    }
}
