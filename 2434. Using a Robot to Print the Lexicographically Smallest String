import java.util.*;

class Solution {
    public String robotWithString(String s) {
        int[] freq = new int[26];
        for (char c : s.toCharArray()) {
            freq[c - 'a']++;
        }

        Stack<Character> stack = new Stack<>();
        StringBuilder result = new StringBuilder();
        int minCharIndex = 0;

        for (char c : s.toCharArray()) {
            stack.push(c);
            freq[c - 'a']--;

            // Update minCharIndex to reflect smallest remaining character
            while (minCharIndex < 26 && freq[minCharIndex] == 0) {
                minCharIndex++;
            }

            // Write from stack if top is ≤ smallest remaining character
            while (!stack.isEmpty() && stack.peek() - 'a' <= minCharIndex) {
                result.append(stack.pop());
            }
        }

        // Write remaining characters from stack
        while (!stack.isEmpty()) {
            result.append(stack.pop());
        }

        return result.toString();
    }
}
